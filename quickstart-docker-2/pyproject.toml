[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "quickstart-docker-2"
version = "1.0.0"
description = ""
license = "Apache-2.0"
dependencies = [
    "flwr[simulation]>=1.17.0",
    "flwr-datasets[vision]>=0.5.0",
    "tensorflow>=2.11.1,<2.18.0",
    "pandas",
    "keras",
    "scikit-learn"
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.flwr.app]	
publisher = "flower"

[tool.flwr.app.components]
serverapp = "quickstart_docker_2.server_app:app"
clientapp = "quickstart_docker_2.client_app:app"

[tool.flwr.app.config]
num-server-rounds = 5
local-epochs = 5
batch-size = 32
verbose = false

[tool.flwr.federations.local-deployment]
address = "127.0.0.1:9093"
insecure = true
